.DS_Store
node_modules/
.clasprc.json

# Contains real personal financial screenshots — never commit to a public repo.
UI references/

# Apps Script project ids (script + spreadsheet) — local only.
backend/.clasp.json
